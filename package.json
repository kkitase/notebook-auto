{
  "name": "notebooklm-url-auto-add",
  "version": "2.0.0",
  "description": "NotebookLMにURLを自動的に追加するNode.jsアプリ (Playwright版)",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "start": "node index.js"
  },
  "dependencies": {
    "playwright": "^1.57.0"
  },
  "bin": {
    "notebook-auto": "./index.js"
  }
}
